# за основу берем образ
FROM python:3.6

# выполнить команду создать дирректорию
RUN mkdir /opt/app
# сделать эту папку рабочей
WORKDIR /opt/app

# взять файл из текущей папки и сохранить в папке /opt/app текущего образа
COPY hello.py /opt/app
# Запустим наш скрипт , выполним эти команды
CMD ["python", "hello.py"]

